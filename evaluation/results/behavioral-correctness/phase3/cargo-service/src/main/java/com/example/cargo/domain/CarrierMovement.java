package com.example.cargo.domain;
//Autogenerated code for DataObject: CarrierMovement
  
import jakarta.persistence.Embeddable;
import jakarta.persistence.Column;
import jakarta.persistence.Embedded;
import jakarta.persistence.AttributeOverrides;
import jakarta.persistence.AttributeOverride;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import jakarta.persistence.PrePersist;
import jakarta.persistence.PreUpdate;

@Embeddable
@Data
@NoArgsConstructor
@AllArgsConstructor
public class CarrierMovement {
  @Embedded
  @AttributeOverrides({
      @AttributeOverride(name = "portCode", column = @Column(name = "destination_portCode")),
  })
  private SharedLocation destination;

  @Embedded
  @AttributeOverrides({
      @AttributeOverride(name = "portCode", column = @Column(name = "source_portCode")),
  })
  private SharedLocation source;

@PreUpdate
@PrePersist
  public void validate(){
      if (!(this.source.getPortCode() != this.destination.getPortCode())) {
         throw new IllegalArgumentException("Violating rule 'Source and Destination must be different'");
       }

  }

  }
