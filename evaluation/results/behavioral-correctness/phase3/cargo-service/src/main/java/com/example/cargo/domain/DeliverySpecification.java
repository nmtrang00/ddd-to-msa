package com.example.cargo.domain;
//Autogenerated code for DataObject: DeliverySpecification
  
import jakarta.persistence.Embeddable;
import jakarta.persistence.Column;
import jakarta.persistence.Embedded;
import jakarta.persistence.AttributeOverrides;
import jakarta.persistence.AttributeOverride;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import jakarta.persistence.PrePersist;
import jakarta.persistence.PreUpdate;

@Embeddable
@Data
@NoArgsConstructor
@AllArgsConstructor
public class DeliverySpecification {
  @Column(nullable = false)
  private Integer deadline;

  @Embedded
  @AttributeOverrides({
      @AttributeOverride(name = "destination", column = @Column(name = "delivery_destination")),
      @AttributeOverride(name = "source", column = @Column(name = "delivery_source")),
  })
  private CarrierMovement delivery;

@PreUpdate
@PrePersist
  public void validate(){
      if (!(this.deadline > 0.0)) {
         throw new IllegalArgumentException("Violating rule 'Deadline must be a positive integer'");
       }

      if (this.delivery != null) {
          this.delivery.validate();
      }
  }

  }
