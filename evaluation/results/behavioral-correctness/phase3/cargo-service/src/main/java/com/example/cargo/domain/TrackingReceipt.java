package com.example.cargo.domain;
//Autogenerated code for DataObject: TrackingReceipt
  
import jakarta.persistence.Embeddable;
import jakarta.persistence.Column;
import jakarta.persistence.Enumerated;
import jakarta.persistence.EnumType;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.CascadeType;
import jakarta.persistence.OneToMany;
import jakarta.persistence.Embedded;
import jakarta.persistence.AttributeOverrides;
import jakarta.persistence.AttributeOverride;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import java.util.List;
import java.util.ArrayList;

@Embeddable
@Data
@NoArgsConstructor
@AllArgsConstructor
public class TrackingReceipt {
  @Embedded
  @AttributeOverrides({
      @AttributeOverride(name = "portCode", column = @Column(name = "lastLocation_portCode")),
  })
  private SharedLocation lastLocation;

  @Column(nullable = false)
  private Integer trackingId;

  @Enumerated(EnumType.STRING)
  @Column(nullable = false)
  private TransportStatus status;

  @OneToMany(cascade = CascadeType.ALL, orphanRemoval = true)
  @JoinColumn(name = "handlingEventId")
  private List<HandlingEvent> events = new ArrayList<>();

  public TrackingReceipt(List<HandlingEvent> events, TransportStatus status, Integer trackingId){
    this.trackingId = trackingId;
    this.status = status;
    this.events = events;
  }

  }
